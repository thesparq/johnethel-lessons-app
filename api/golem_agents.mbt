// Generated by golem_sdk_tools — DO NOT EDIT!

fn init{
  @agents.register_agent({
    name: "ApiAgent",
    agent_type: {
      type_name: "ApiAgent",
      description: "API entry point agent — ephemeral, one per request",
      source_language: "moonbit",
      constructor_: {
        name: Some("new"),
        description: "Creates a new ApiAgent",
        prompt_hint: None,
        input_schema: @rpcTypes.DataSchema::Tuple([]),
      },
      methods: [
        {
          name: "get_subjects",
          description: "List all subjects — open to everyone",
          http_endpoint: [
            {
              http_method: @common.HttpMethod::Get,
              path_suffix: [@common.PathSegment::Literal("subjects")],
              header_vars: [
                { header_name: "Authorization", variable_name: "auth_header" },
              ],
              query_vars: [],
              auth_details: None,
              cors_options: { allowed_patterns: [], },
            },
          ],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "auth_header",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
        },
        {
          name: "get_lessons",
          description: "List lessons for a subject — open to everyone",
          http_endpoint: [
            {
              http_method: @common.HttpMethod::Get,
              path_suffix: [
                @common.PathSegment::Literal("subjects"),
                @common.PathSegment::PathVariable({
                  variable_name: "subject_id",
                }),
                @common.PathSegment::Literal("lessons"),
              ],
              header_vars: [
                { header_name: "Authorization", variable_name: "auth_header" },
              ],
              query_vars: [],
              auth_details: None,
              cors_options: { allowed_patterns: [], },
            },
          ],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "subject_id",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
            (
              "auth_header",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
        },
        {
          name: "get_lesson",
          description: "Get lesson content — open to everyone",
          http_endpoint: [
            {
              http_method: @common.HttpMethod::Get,
              path_suffix: [
                @common.PathSegment::Literal("lessons"),
                @common.PathSegment::PathVariable({
                  variable_name: "lesson_id",
                }),
              ],
              header_vars: [
                { header_name: "Authorization", variable_name: "auth_header" },
              ],
              query_vars: [],
              auth_details: None,
              cors_options: { allowed_patterns: [], },
            },
          ],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "lesson_id",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
            (
              "auth_header",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
        },
        {
          name: "toggle_lesson",
          description: "Toggle lesson active state (teacher or admin only)  Routes to UserAgent which updates SurrealDB directly.",
          http_endpoint: [
            {
              http_method: @common.HttpMethod::Post,
              path_suffix: [
                @common.PathSegment::Literal("lessons"),
                @common.PathSegment::PathVariable({
                  variable_name: "lesson_id",
                }),
                @common.PathSegment::Literal("toggle"),
              ],
              header_vars: [
                { header_name: "Authorization", variable_name: "auth_header" },
              ],
              query_vars: [],
              auth_details: None,
              cors_options: { allowed_patterns: [], },
            },
          ],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "lesson_id",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
            (
              "auth_header",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
        },
        {
          name: "assign_teacher",
          description: "Assign teacher to subject (admin only)",
          http_endpoint: [
            {
              http_method: @common.HttpMethod::Post,
              path_suffix: [
                @common.PathSegment::Literal("admin"),
                @common.PathSegment::Literal("assign"),
              ],
              header_vars: [
                { header_name: "Authorization", variable_name: "auth_header" },
              ],
              query_vars: [
                { query_param_name: "teacher_id", variable_name: "teacher_id" },
                { query_param_name: "subject", variable_name: "subject" },
              ],
              auth_details: None,
              cors_options: { allowed_patterns: [], },
            },
          ],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "teacher_id",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
            (
              "subject",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
            (
              "auth_header",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
        },
      ],
      dependencies: [],
      mode: @common.AgentMode::EPHEMERAL,
      http_mount: Some({
        path_prefix: [],
        auth_details: None,
        phantom_agent: false,
        cors_options: { allowed_patterns: ["*"], },
        webhook_suffix: [],
      }),
      snapshotting: @common.Snapshotting::Disabled,
      config: [],
    },
    construct: fn(input, principal) {
      try {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 0 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 0 elements, got " + elements.length().to_string(),
          )
        }
        let agent = ApiAgent::new()
        let constructed = @agents.ConstructedAgent::{
          raw: agent,
          snapshottable: None,
          snapshot_format: None,
        }
        Ok(constructed)
      } catch{
        e => Err(e)
      }
    },
  })
}

///|
impl @agents.RawAgent for ApiAgent with invoke(
  self,
  method_name,
  input,
  principal,
) {
  try {
    match method_name {
      "get_subjects" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 1 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 1 elements, got " + elements.length().to_string(),
          )
        }
        let auth_header :String = @schema.from_element_value_as(elements[0])
        let result = self.get_subjects(auth_header)
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      "get_lessons" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 2 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 2 elements, got " + elements.length().to_string(),
          )
        }
        let subject_id :String = @schema.from_element_value_as(elements[0])
        let auth_header :String = @schema.from_element_value_as(elements[1])
        let result = self.get_lessons(subject_id, auth_header)
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      "get_lesson" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 2 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 2 elements, got " + elements.length().to_string(),
          )
        }
        let lesson_id :String = @schema.from_element_value_as(elements[0])
        let auth_header :String = @schema.from_element_value_as(elements[1])
        let result = self.get_lesson(lesson_id, auth_header)
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      "toggle_lesson" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 2 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 2 elements, got " + elements.length().to_string(),
          )
        }
        let lesson_id :String = @schema.from_element_value_as(elements[0])
        let auth_header :String = @schema.from_element_value_as(elements[1])
        let result = self.toggle_lesson(lesson_id, auth_header)
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      "assign_teacher" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 3 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 3 elements, got " + elements.length().to_string(),
          )
        }
        let teacher_id :String = @schema.from_element_value_as(elements[0])
        let subject :String = @schema.from_element_value_as(elements[1])
        let auth_header :String = @schema.from_element_value_as(elements[2])
        let result = self.assign_teacher(teacher_id, subject, auth_header)
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      other => Err(@common.AgentError::InvalidMethod(other))
    }
  } catch{
    e => Err(e)
  }
}

