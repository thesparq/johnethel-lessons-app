// Generated by golem_sdk_tools — DO NOT EDIT!

///|
pub(all) struct ApiAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn ApiAgentClient::new_phantom() -> ApiAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = @rpc.AgentClient::new_phantom("ApiAgent", constructor_data)
  { client, }
}

///|
pub fn ApiAgentClient::get_phantom(phantom_id : @rpcTypes.Uuid) -> ApiAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = 
    @rpc.AgentClient::get_phantom("ApiAgent", constructor_data, phantom_id)
  { client, }
}

///|
pub fn ApiAgentClient::get_agent_id(self : ApiAgentClient) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn ApiAgentClient::phantom_id(self : ApiAgentClient) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn ApiAgentClient::drop(self : ApiAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] ApiAgentClient::scoped_new_phantom(
  f : (ApiAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = ApiAgentClient::new_phantom()
  defer client.drop()
  f(client)
}

///|
pub fn[T] ApiAgentClient::scoped_get_phantom(
  phantom_id : @rpcTypes.Uuid,
  f : (ApiAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = ApiAgentClient::get_phantom(phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn ApiAgentClient::get_subjects(self : ApiAgentClient, auth_header : String) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(auth_header)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn ApiAgentClient::trigger_get_subjects(
  self : ApiAgentClient,
  auth_header : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(auth_header)]),
    )
}

///|
pub fn ApiAgentClient::schedule_get_subjects(
  self : ApiAgentClient,
  scheduled_at : @wallClock.Datetime,
  auth_header : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(auth_header)]),
    )
}

///|
pub fn ApiAgentClient::schedule_cancelable_get_subjects(
  self : ApiAgentClient,
  scheduled_at : @wallClock.Datetime,
  auth_header : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(auth_header)]),
    )
}

///|
pub fn ApiAgentClient::get_lessons(
  self : ApiAgentClient,
  subject_id : String,
  auth_header : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(auth_header),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn ApiAgentClient::trigger_get_lessons(
  self : ApiAgentClient,
  subject_id : String,
  auth_header : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(auth_header),
      ]),
    )
}

///|
pub fn ApiAgentClient::schedule_get_lessons(
  self : ApiAgentClient,
  scheduled_at : @wallClock.Datetime,
  subject_id : String,
  auth_header : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(auth_header),
      ]),
    )
}

///|
pub fn ApiAgentClient::schedule_cancelable_get_lessons(
  self : ApiAgentClient,
  scheduled_at : @wallClock.Datetime,
  subject_id : String,
  auth_header : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(auth_header),
      ]),
    )
}

///|
pub fn ApiAgentClient::get_lesson(
  self : ApiAgentClient,
  lesson_id : String,
  auth_header : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_lesson",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(lesson_id),
        @schema.to_element_value_as(auth_header),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn ApiAgentClient::trigger_get_lesson(
  self : ApiAgentClient,
  lesson_id : String,
  auth_header : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_lesson",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(lesson_id),
        @schema.to_element_value_as(auth_header),
      ]),
    )
}

///|
pub fn ApiAgentClient::schedule_get_lesson(
  self : ApiAgentClient,
  scheduled_at : @wallClock.Datetime,
  lesson_id : String,
  auth_header : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_lesson",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(lesson_id),
        @schema.to_element_value_as(auth_header),
      ]),
    )
}

///|
pub fn ApiAgentClient::schedule_cancelable_get_lesson(
  self : ApiAgentClient,
  scheduled_at : @wallClock.Datetime,
  lesson_id : String,
  auth_header : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_lesson",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(lesson_id),
        @schema.to_element_value_as(auth_header),
      ]),
    )
}

///|
pub fn ApiAgentClient::toggle_lesson(
  self : ApiAgentClient,
  lesson_id : String,
  auth_header : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "toggle_lesson",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(lesson_id),
        @schema.to_element_value_as(auth_header),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn ApiAgentClient::trigger_toggle_lesson(
  self : ApiAgentClient,
  lesson_id : String,
  auth_header : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "toggle_lesson",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(lesson_id),
        @schema.to_element_value_as(auth_header),
      ]),
    )
}

///|
pub fn ApiAgentClient::schedule_toggle_lesson(
  self : ApiAgentClient,
  scheduled_at : @wallClock.Datetime,
  lesson_id : String,
  auth_header : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "toggle_lesson",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(lesson_id),
        @schema.to_element_value_as(auth_header),
      ]),
    )
}

///|
pub fn ApiAgentClient::schedule_cancelable_toggle_lesson(
  self : ApiAgentClient,
  scheduled_at : @wallClock.Datetime,
  lesson_id : String,
  auth_header : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "toggle_lesson",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(lesson_id),
        @schema.to_element_value_as(auth_header),
      ]),
    )
}

///|
pub fn ApiAgentClient::assign_teacher(
  self : ApiAgentClient,
  teacher_id : String,
  subject : String,
  auth_header : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "assign_teacher",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(teacher_id),
        @schema.to_element_value_as(subject),
        @schema.to_element_value_as(auth_header),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn ApiAgentClient::trigger_assign_teacher(
  self : ApiAgentClient,
  teacher_id : String,
  subject : String,
  auth_header : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "assign_teacher",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(teacher_id),
        @schema.to_element_value_as(subject),
        @schema.to_element_value_as(auth_header),
      ]),
    )
}

///|
pub fn ApiAgentClient::schedule_assign_teacher(
  self : ApiAgentClient,
  scheduled_at : @wallClock.Datetime,
  teacher_id : String,
  subject : String,
  auth_header : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "assign_teacher",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(teacher_id),
        @schema.to_element_value_as(subject),
        @schema.to_element_value_as(auth_header),
      ]),
    )
}

///|
pub fn ApiAgentClient::schedule_cancelable_assign_teacher(
  self : ApiAgentClient,
  scheduled_at : @wallClock.Datetime,
  teacher_id : String,
  subject : String,
  auth_header : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "assign_teacher",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(teacher_id),
        @schema.to_element_value_as(subject),
        @schema.to_element_value_as(auth_header),
      ]),
    )
}

