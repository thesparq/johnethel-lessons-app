// Generated by golem_sdk_tools — DO NOT EDIT!

///|
pub(all) struct QueryForkClient {
  client : @rpc.AgentClient
}

///|
pub fn QueryForkClient::new_phantom(sql : String) -> QueryForkClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(sql)])
  let client = @rpc.AgentClient::new_phantom("QueryFork", constructor_data)
  { client, }
}

///|
pub fn QueryForkClient::get_phantom(sql : String, phantom_id : @rpcTypes.Uuid) -> QueryForkClient raise @common.AgentError {
  let constructor_data = 
    @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(sql)])
  let client = 
    @rpc.AgentClient::get_phantom("QueryFork", constructor_data, phantom_id)
  { client, }
}

///|
pub fn QueryForkClient::get_agent_id(self : QueryForkClient) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn QueryForkClient::phantom_id(self : QueryForkClient) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn QueryForkClient::drop(self : QueryForkClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] QueryForkClient::scoped_new_phantom(
  sql : String,
  f : (QueryForkClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = QueryForkClient::new_phantom(sql)
  defer client.drop()
  f(client)
}

///|
pub fn[T] QueryForkClient::scoped_get_phantom(
  sql : String,
  phantom_id : @rpcTypes.Uuid,
  f : (QueryForkClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = QueryForkClient::get_phantom(sql, phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn QueryForkClient::execute(self : QueryForkClient) -> String raise @common.AgentError {
  let result = 
    self.client.invoke_and_await("execute", @rpcTypes.DataValue::Tuple([]))
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn QueryForkClient::trigger_execute(self : QueryForkClient) -> Unit {
  self.client.invoke("execute", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn QueryForkClient::schedule_execute(
  self : QueryForkClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "execute",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub fn QueryForkClient::schedule_cancelable_execute(
  self : QueryForkClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "execute",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub(all) struct UserAgentClient {
  client : @rpc.AgentClient
}

///|
pub fn UserAgentClient::get() -> UserAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = @rpc.AgentClient::get("UserAgent", constructor_data)
  { client, }
}

///|
pub fn UserAgentClient::new_phantom() -> UserAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = @rpc.AgentClient::new_phantom("UserAgent", constructor_data)
  { client, }
}

///|
pub fn UserAgentClient::get_phantom(phantom_id : @rpcTypes.Uuid) -> UserAgentClient raise @common.AgentError {
  let constructor_data = @rpcTypes.DataValue::Tuple([])
  let client = 
    @rpc.AgentClient::get_phantom("UserAgent", constructor_data, phantom_id)
  { client, }
}

///|
pub fn UserAgentClient::get_agent_id(self : UserAgentClient) -> String raise @common.AgentError {
  self.client.get_agent_id()
}

///|
pub fn UserAgentClient::phantom_id(self : UserAgentClient) -> @rpcTypes.Uuid? {
  self.client.phantom_id()
}

///|
pub fn UserAgentClient::drop(self : UserAgentClient) -> Unit {
  self.client.drop()
}

///|
pub fn[T] UserAgentClient::scoped(
  f : (UserAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = UserAgentClient::get()
  defer client.drop()
  f(client)
}

///|
pub fn[T] UserAgentClient::scoped_new_phantom(
  f : (UserAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = UserAgentClient::new_phantom()
  defer client.drop()
  f(client)
}

///|
pub fn[T] UserAgentClient::scoped_get_phantom(
  phantom_id : @rpcTypes.Uuid,
  f : (UserAgentClient) -> T raise @common.AgentError,
) -> T raise @common.AgentError {
  let client = UserAgentClient::get_phantom(phantom_id)
  defer client.drop()
  f(client)
}

///|
pub fn UserAgentClient::get_classes(
  self : UserAgentClient,
  class_level : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_classes",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn UserAgentClient::trigger_get_classes(
  self : UserAgentClient,
  class_level : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_classes",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
    )
}

///|
pub fn UserAgentClient::schedule_get_classes(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  class_level : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_classes",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
    )
}

///|
pub fn UserAgentClient::schedule_cancelable_get_classes(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  class_level : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_classes",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
    )
}

///|
pub fn UserAgentClient::get_subjects(
  self : UserAgentClient,
  class_level : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn UserAgentClient::trigger_get_subjects(
  self : UserAgentClient,
  class_level : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
    )
}

///|
pub fn UserAgentClient::schedule_get_subjects(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  class_level : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
    )
}

///|
pub fn UserAgentClient::schedule_cancelable_get_subjects(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  class_level : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
    )
}

///|
pub fn UserAgentClient::get_terms(
  self : UserAgentClient,
  class_level : String,
  subject : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_terms",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(subject),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn UserAgentClient::trigger_get_terms(
  self : UserAgentClient,
  class_level : String,
  subject : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_terms",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(subject),
      ]),
    )
}

///|
pub fn UserAgentClient::schedule_get_terms(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  class_level : String,
  subject : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_terms",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(subject),
      ]),
    )
}

///|
pub fn UserAgentClient::schedule_cancelable_get_terms(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  class_level : String,
  subject : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_terms",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(subject),
      ]),
    )
}

///|
pub fn UserAgentClient::get_lessons(
  self : UserAgentClient,
  subject : String,
  class_level : String,
  term : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(subject),
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(term),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn UserAgentClient::trigger_get_lessons(
  self : UserAgentClient,
  subject : String,
  class_level : String,
  term : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(subject),
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(term),
      ]),
    )
}

///|
pub fn UserAgentClient::schedule_get_lessons(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  subject : String,
  class_level : String,
  term : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(subject),
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(term),
      ]),
    )
}

///|
pub fn UserAgentClient::schedule_cancelable_get_lessons(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  subject : String,
  class_level : String,
  term : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(subject),
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(term),
      ]),
    )
}

///|
pub fn UserAgentClient::get_lesson(self : UserAgentClient, lesson_id : String) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "get_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn UserAgentClient::trigger_get_lesson(
  self : UserAgentClient,
  lesson_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "get_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
}

///|
pub fn UserAgentClient::schedule_get_lesson(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  lesson_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "get_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
}

///|
pub fn UserAgentClient::schedule_cancelable_get_lesson(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  lesson_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "get_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
}

///|
pub fn UserAgentClient::toggle_lesson(
  self : UserAgentClient,
  lesson_id : String,
) -> Bool raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "toggle_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn UserAgentClient::trigger_toggle_lesson(
  self : UserAgentClient,
  lesson_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "toggle_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
}

///|
pub fn UserAgentClient::schedule_toggle_lesson(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  lesson_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "toggle_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
}

///|
pub fn UserAgentClient::schedule_cancelable_toggle_lesson(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  lesson_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "toggle_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
}

///|
pub fn UserAgentClient::http_config(self : UserAgentClient) -> String raise @common.AgentError {
  let result = 
    self.client.invoke_and_await("http_config", @rpcTypes.DataValue::Tuple([]))
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn UserAgentClient::trigger_http_config(self : UserAgentClient) -> Unit {
  self.client.invoke("http_config", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn UserAgentClient::schedule_http_config(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "http_config",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub fn UserAgentClient::schedule_cancelable_http_config(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "http_config",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub fn UserAgentClient::http_classes(self : UserAgentClient) -> String raise @common.AgentError {
  let result = 
    self.client.invoke_and_await("http_classes", @rpcTypes.DataValue::Tuple([]))
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn UserAgentClient::trigger_http_classes(self : UserAgentClient) -> Unit {
  self.client.invoke("http_classes", @rpcTypes.DataValue::Tuple([]))
}

///|
pub fn UserAgentClient::schedule_http_classes(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> Unit {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "http_classes",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub fn UserAgentClient::schedule_cancelable_http_classes(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
) -> @agentHost.CancellationToken {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "http_classes",
      @rpcTypes.DataValue::Tuple([]),
    )
}

///|
pub fn UserAgentClient::http_subjects(
  self : UserAgentClient,
  class_level : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "http_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn UserAgentClient::trigger_http_subjects(
  self : UserAgentClient,
  class_level : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "http_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
    )
}

///|
pub fn UserAgentClient::schedule_http_subjects(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  class_level : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "http_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
    )
}

///|
pub fn UserAgentClient::schedule_cancelable_http_subjects(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  class_level : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "http_subjects",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(class_level)]),
    )
}

///|
pub fn UserAgentClient::http_terms(
  self : UserAgentClient,
  class_level : String,
  subject : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "http_terms",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(subject),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn UserAgentClient::trigger_http_terms(
  self : UserAgentClient,
  class_level : String,
  subject : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "http_terms",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(subject),
      ]),
    )
}

///|
pub fn UserAgentClient::schedule_http_terms(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  class_level : String,
  subject : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "http_terms",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(subject),
      ]),
    )
}

///|
pub fn UserAgentClient::schedule_cancelable_http_terms(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  class_level : String,
  subject : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "http_terms",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(subject),
      ]),
    )
}

///|
pub fn UserAgentClient::http_lessons(
  self : UserAgentClient,
  subject_id : String,
  class_level : String,
  term : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "http_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(term),
      ]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn UserAgentClient::trigger_http_lessons(
  self : UserAgentClient,
  subject_id : String,
  class_level : String,
  term : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "http_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(term),
      ]),
    )
}

///|
pub fn UserAgentClient::schedule_http_lessons(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  subject_id : String,
  class_level : String,
  term : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "http_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(term),
      ]),
    )
}

///|
pub fn UserAgentClient::schedule_cancelable_http_lessons(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  subject_id : String,
  class_level : String,
  term : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "http_lessons",
      @rpcTypes.DataValue::Tuple([
        @schema.to_element_value_as(subject_id),
        @schema.to_element_value_as(class_level),
        @schema.to_element_value_as(term),
      ]),
    )
}

///|
pub fn UserAgentClient::http_lesson(self : UserAgentClient, lesson_id : String) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "http_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn UserAgentClient::trigger_http_lesson(
  self : UserAgentClient,
  lesson_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "http_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
}

///|
pub fn UserAgentClient::schedule_http_lesson(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  lesson_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "http_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
}

///|
pub fn UserAgentClient::schedule_cancelable_http_lesson(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  lesson_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "http_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
}

///|
pub fn UserAgentClient::http_toggle_lesson(
  self : UserAgentClient,
  lesson_id : String,
) -> String raise @common.AgentError {
  let result = self
    .client
    .invoke_and_await(
      "http_toggle_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
  let elements = @extractor.extract_tuple(result)
  @schema.from_element_value_as(elements[0])
}

///|
pub fn UserAgentClient::trigger_http_toggle_lesson(
  self : UserAgentClient,
  lesson_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .invoke(
      "http_toggle_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
}

///|
pub fn UserAgentClient::schedule_http_toggle_lesson(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  lesson_id : String,
) -> Unit raise @common.AgentError {
  self
    .client
    .schedule_invocation(
      scheduled_at,
      "http_toggle_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
}

///|
pub fn UserAgentClient::schedule_cancelable_http_toggle_lesson(
  self : UserAgentClient,
  scheduled_at : @wallClock.Datetime,
  lesson_id : String,
) -> @agentHost.CancellationToken raise @common.AgentError {
  self
    .client
    .schedule_cancelable_invocation(
      scheduled_at,
      "http_toggle_lesson",
      @rpcTypes.DataValue::Tuple([@schema.to_element_value_as(lesson_id)]),
    )
}

