// Generated by golem_sdk_tools — DO NOT EDIT!

fn init{
  @agents.register_agent({
    name: "QueryFork",
    agent_type: {
      type_name: "QueryFork",
      description: "QueryFork — ephemeral SurrealDB worker.  Makes blocking HTTP requests to SurrealDB, returns JSON string, then dies. Isolates blocking I/O from durable UserAgent.",
      source_language: "moonbit",
      constructor_: {
        name: Some("new"),
        description: "Creates a new QueryFork",
        prompt_hint: None,
        input_schema: @rpcTypes.DataSchema::Tuple([
          (
            "sql",
            @schema.schema_of_tag(
              (@schema.TypeTag::{  } : @schema.TypeTag[String]),
            ),
          ),
        ]),
      },
      methods: [
        {
          name: "execute",
          description: "Execute the SQL query against SurrealDB and return the JSON result string.",
          http_endpoint: [],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
        },
      ],
      dependencies: [],
      mode: @common.AgentMode::EPHEMERAL,
      http_mount: None,
      snapshotting: @common.Snapshotting::Disabled,
      config: [],
    },
    construct: fn(input, principal) {
      try {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 1 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 1 elements, got " + elements.length().to_string(),
          )
        }
        let sql :String = @schema.from_element_value_as(elements[0])
        let agent = QueryFork::new(sql)
        let constructed = @agents.ConstructedAgent::{
          raw: agent,
          snapshottable: None,
          snapshot_format: None,
        }
        Ok(constructed)
      } catch{
        e => Err(e)
      }
    },
  })
  @agents.register_agent({
    name: "UserAgent",
    agent_type: {
      type_name: "UserAgent",
      description: "User agent — durable per-user instance with caching.",
      source_language: "moonbit",
      constructor_: {
        name: Some("new"),
        description: "Creates a new UserAgent",
        prompt_hint: None,
        input_schema: @rpcTypes.DataSchema::Tuple([
          (
            "user_id",
            @schema.schema_of_tag(
              (@schema.TypeTag::{  } : @schema.TypeTag[String]),
            ),
          ),
          (
            "role",
            @schema.schema_of_tag(
              (@schema.TypeTag::{  } : @schema.TypeTag[String]),
            ),
          ),
        ]),
      },
      methods: [
        {
          name: "get_classes",
          description: "get_classes",
          http_endpoint: [],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "class_level",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
        },
        {
          name: "get_terms",
          description: "get_terms",
          http_endpoint: [],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "class_level",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
            (
              "subject",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
        },
        {
          name: "get_subjects",
          description: "get_subjects",
          http_endpoint: [],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "class_level",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
        },
        {
          name: "get_lessons",
          description: "get_lessons",
          http_endpoint: [],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "subject",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
            (
              "class_level",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
            (
              "term",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
        },
        {
          name: "get_lesson",
          description: "get_lesson",
          http_endpoint: [],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "lesson_id",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
        },
        {
          name: "toggle_lesson",
          description: "Toggle lesson active state (teacher or admin only).  Updates SurrealDB directly. Returns new active value.",
          http_endpoint: [],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "lesson_id",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[Bool]),
              ),
            ),
          ]),
        },
      ],
      dependencies: [],
      mode: @common.AgentMode::DURABLE,
      http_mount: None,
      snapshotting: @common.Snapshotting::Disabled,
      config: [],
    },
    construct: fn(input, principal) {
      try {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 2 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 2 elements, got " + elements.length().to_string(),
          )
        }
        let user_id :String = @schema.from_element_value_as(elements[0])
        let role :String = @schema.from_element_value_as(elements[1])
        let agent = UserAgent::new(user_id, role)
        let constructed = @agents.ConstructedAgent::{
          raw: agent,
          snapshottable: None,
          snapshot_format: None,
        }
        Ok(constructed)
      } catch{
        e => Err(e)
      }
    },
  })
}

///|
impl @agents.RawAgent for QueryFork with invoke(
  self,
  method_name,
  input,
  principal,
) {
  try {
    match method_name {
      "execute" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 0 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 0 elements, got " + elements.length().to_string(),
          )
        }
        let result = self.execute()
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      other => Err(@common.AgentError::InvalidMethod(other))
    }
  } catch{
    e => Err(e)
  }
}

///|
impl @agents.RawAgent for UserAgent with invoke(
  self,
  method_name,
  input,
  principal,
) {
  try {
    match method_name {
      "get_classes" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 1 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 1 elements, got " + elements.length().to_string(),
          )
        }
        let class_level :String = @schema.from_element_value_as(elements[0])
        let result = self.get_classes(class_level)
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      "get_terms" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 2 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 2 elements, got " + elements.length().to_string(),
          )
        }
        let class_level :String = @schema.from_element_value_as(elements[0])
        let subject :String = @schema.from_element_value_as(elements[1])
        let result = self.get_terms(class_level, subject)
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      "get_subjects" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 1 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 1 elements, got " + elements.length().to_string(),
          )
        }
        let class_level :String = @schema.from_element_value_as(elements[0])
        let result = self.get_subjects(class_level)
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      "get_lessons" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 3 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 3 elements, got " + elements.length().to_string(),
          )
        }
        let subject :String = @schema.from_element_value_as(elements[0])
        let class_level :String = @schema.from_element_value_as(elements[1])
        let term :String = @schema.from_element_value_as(elements[2])
        let result = self.get_lessons(subject, class_level, term)
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      "get_lesson" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 1 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 1 elements, got " + elements.length().to_string(),
          )
        }
        let lesson_id :String = @schema.from_element_value_as(elements[0])
        let result = self.get_lesson(lesson_id)
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      "toggle_lesson" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 1 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 1 elements, got " + elements.length().to_string(),
          )
        }
        let lesson_id :String = @schema.from_element_value_as(elements[0])
        let result = self.toggle_lesson(lesson_id)
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      other => Err(@common.AgentError::InvalidMethod(other))
    }
  } catch{
    e => Err(e)
  }
}

