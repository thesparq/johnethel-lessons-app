// Generated by golem_sdk_tools — DO NOT EDIT!

fn init{
  @agents.register_agent({
    name: "FileServerAgent",
    agent_type: {
      type_name: "FileServerAgent",
      description: "Static file server agent — ephemeral (stateless)",
      source_language: "moonbit",
      constructor_: {
        name: Some("new"),
        description: "Creates a new FileServerAgent",
        prompt_hint: None,
        input_schema: @rpcTypes.DataSchema::Tuple([]),
      },
      methods: [
        {
          name: "download_file",
          description: "Serve static files. Falls back to index.html for SPA routing.  Special handling for /config.js — generated from env vars.",
          http_endpoint: [
            {
              http_method: @common.HttpMethod::Get,
              path_suffix: [
                @common.PathSegment::RemainingPathVariable({
                  variable_name: "path",
                }),
              ],
              header_vars: [],
              query_vars: [],
              auth_details: None,
              cors_options: { allowed_patterns: [], },
            },
          ],
          prompt_hint: None,
          input_schema: @rpcTypes.DataSchema::Tuple([
            (
              "path",
              @schema.schema_of_tag(
                (@schema.TypeTag::{  } : @schema.TypeTag[String]),
              ),
            ),
          ]),
          output_schema: @rpcTypes.DataSchema::Tuple([
            (
              "result",
              @schema.schema_of_tag(
                (
                  @schema.TypeTag::{  }
                  : @schema.TypeTag[@types.UnstructuredBinary]
                ),
              ),
            ),
          ]),
        },
      ],
      dependencies: [],
      mode: @common.AgentMode::EPHEMERAL,
      http_mount: Some({
        path_prefix: [],
        auth_details: None,
        phantom_agent: false,
        cors_options: { allowed_patterns: [], },
        webhook_suffix: [],
      }),
      snapshotting: @common.Snapshotting::Disabled,
      config: [],
    },
    construct: fn(input, principal) {
      try {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 0 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 0 elements, got " + elements.length().to_string(),
          )
        }
        let agent = FileServerAgent::new()
        let constructed = @agents.ConstructedAgent::{
          raw: agent,
          snapshottable: None,
          snapshot_format: None,
        }
        Ok(constructed)
      } catch{
        e => Err(e)
      }
    },
  })
}

///|
impl @agents.RawAgent for FileServerAgent with invoke(
  self,
  method_name,
  input,
  principal,
) {
  try {
    match method_name {
      "download_file" => {
        let elements = @extractor.extract_tuple(input)
        guard elements.length() == 1 else {
          raise  @common.AgentError::InvalidInput(
            "Expected 1 elements, got " + elements.length().to_string(),
          )
        }
        let path :String = @schema.from_element_value_as(elements[0])
        let result = self.download_file(path)
        Ok(@rpcTypes.DataValue::Tuple([@schema.to_element_value_as(result)]))
      }
      other => Err(@common.AgentError::InvalidMethod(other))
    }
  } catch{
    e => Err(e)
  }
}

